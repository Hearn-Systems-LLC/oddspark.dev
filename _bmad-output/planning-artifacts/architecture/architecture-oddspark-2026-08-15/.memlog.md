---
scope: Oddspark coherence-gated Opportunity Brief pipeline (brownfield worker)
purpose: Keep two independently built units (generation pipeline, preserved worker shell) from diverging
altitude: feature (one product, one worker)
updated: 2026-08-16T13:31
---

- (decision) Fast path; deliverables = spine + one human-reviewable rendering at finalize.
- (decision) Paradigm: pipes-and-filters. Pipeline stages Evidence -> Generate -> Gate -> Commit -> Render, each a single-file section with a typed port; gates are a mandatory stage, never a post-hoc filter. Prevents coherence checks decaying into style nudges.
- (decision) OQ3 resolved: gate evaluation is a SEPARATE judge pass (structured 9-gate verdict JSON, temp ~0), never generator self-certification. Prevents the generator grading its own homework.
- (decision) OQ2 resolved: bounded regeneration = 1 initial + 2 regenerations (3 candidates max per strike); on exhaustion serve a pre-vetted house Brief (curated per season, gate-passing by construction). Bounds LLM cost per press.
- (decision) OQ1 resolved (upgrades PRD quiet stance): cache-first commit preserves the receipt. First gate-passing Brief in a seed window is pinned via existing w:/pw:/claim/commit; LLM nondeterminism only decides the first committed candidate, then everyone gets the same artifact. Same-window-identical holds without claiming model determinism.
- (decision) OQ5 resolved: minimum evidence threshold = profile clarity=clear AND >=1 exact-substring-verified observation AND non-empty detected-capabilities list in the Evidence Bundle; below threshold, fall back to Coherent Local Mode with plain-language notice.
- (decision) OQ6 resolved: Evidence Bundle extends inferWebsiteProfile to a capabilities/channels inventory; judge gate 3 checks candidates against it; exact-substring grounding pattern (worker.js:886) extended to all business-specific claims.
- (decision) OQ4 resolved: analytics are server-side aggregate counters in KV (strikes, fallback rate, invitation clicks via a /api/cheer endpoint) — no per-visitor tracking beyond the declared abuse carve-out.
- (decision) Result card is a typed contract: Brief JSON schema with the 8 contract elements; HTML/text/JSON renderers consume the schema, never free model text. Prevents renderer drift and audit-creep.
- (decision) Preservation seam: pipeline replaces only generate/generatePersonalized internals behind their existing output port; router, KV, DOs, page shell, seed feeds unchanged. Dependency direction: shell -> pipeline, never back.
- (decision) Robots.txt respected during scan; domain-mode Briefs get no public permalink (/s/:id only for local-mode sparks). Resolves privacy-review H4.
- (version) Stack reality-checked: Workers (compat 2026-07-01), Workers AI @cf/openai/gpt-oss-120b primary / gpt-oss-20b fallback (Cloudflare docs current 2026-08), KV, DO sqlite classes NeuronMeter+SparkCoordinator, wrangler vars for model IDs.
- (assumption) Single-file worker with section banners remains the layout convention; pipeline stages become new banner sections rather than new modules.
- (question) OQ7 deferred to pre-launch: judge prompt + gate-9 surprise threshold calibrated against Justin's voice rubric and golden Briefs (PRD FR-5 deliverable).
- (decision) Orchestrator owns the retry loop (adversarial H2): stages single-pass, no stage calls another; per-strike bound 6 model calls.
- (decision) AD-5 fully typed (H1): integer version, mode as single renderer authority, notice + grounded_numbers fields; legacy personalization.status branching removed.
- (decision) Grounding single-owner at schema stage vs canonicalized scan text (H3); grounding failure = retry, never silent mode downgrade; profile_hash preimage replaced with version bump.
- (decision) AD-7 carve-out (H4): COORD /commit validation + SPARK_ID_RE amended for the Brief artifact — the only DO change; house Brief commits like any artifact.
- (decision) STRIKE_BUDGET_MS hard wall-clock bound added to AD-9 (rubric F-1); verification convention added (Node-importable stages, gate harness with contradiction set + golden fixtures).
- (event) Reviewer gate complete: rubric adequate (fixes applied), tech-currency PASS, adversarial 7 holes all closed in spine; lint clean.
- (event) Rendering produced: solution-design.md (human-reviewable companion); editorial structure+prose standards applied, no changes needed.
- (event) spine finalized
- (change) AD-2 amended by approved course correction: Gate is composite local deterministic/privacy-policy validation plus one separate semantic judge; provider output is outer JudgeResult{candidate_ref,verdict}, and only the exact inner CanonicalVerdict{pass,gates[9],tone,claims} maps inward without semantic invention.
- (change) AD-3/AD-9 reconciled to one six-model-call ledger: every evidence/generation/judge invocation consumes before call, and only complete generation-judge pairs start; candidate limit is min(3,floor((6-E)/2)), so E=0 gives three and E=1 gives two.
- (change) AD-4/AD-5 privacy and grounding ownership tightened: mechanically decidable PII and claim/number provenance are local; Story 1.5 owns a non-model tri-state personal-name policy whose fail or unknown result rejects before judging.
- (decision) AD-11 added: generation and judge roles have separate structural identities and staged qualification; Story 1.2 structural fidelity/binding/isolated latency, Story 1.7 generation shape, Story 1.13 semantic calibration, and Story 1.9 full-pair readiness must all be current for production.
- (change) AD-9 role selection is provider-portable: role-qualified selectors share the ledger/deadline; Workers AI retains NeuronMeter, while another provider requires an approved equivalent cost meter/budget and security/operational decision.
- (change) Stack and verification corrected: gpt-oss pair remain generation candidates pending Story 1.7; judge provider/model is unset pending AD-11; verification separates local deterministic fixtures, live structural/binding qualification, semantic calibration, and full-pair readiness.
- (version) Cloudflare Workers AI JSON Mode docs rechecked via Context7 on 2026-08-16: response_format type json_schema shape remains documented, schema compliance is not guaranteed, and neither @cf/openai/gpt-oss-120b nor @cf/openai/gpt-oss-20b appears in the listed supported models.
- (event) Story 1.2 v1 evidence records conservative NO-GO for the tested exact gpt-oss configurations: 0/20 direct-valid and 0/20 post-repair-valid per model; preserve evidence, leave story in progress, and allow one governed structural recovery attempt.
- (direction) solution-design.md must explain the composite Gate, outer candidate binding, staged qualification, dynamic call arithmetic, role-specific selectors/meters, and the Story 1.2 NO-GO without overstating provider provenance.
- (change) Input reconciliation clarified ephemeral scan-buffer PII lifecycle, split unset recovery candidate selection from disabled production judge configuration, and added course-correction/companion provenance.
- (change) Reviewer closure added immutable AttemptContext, exact candidate/evidence hashes, closed GroundingReport and CanonicalVerdict rules, and an explicit versioned CommittedBrief envelope.
- (decision) COORD is the authoritative same-window receipt and atomic analytics owner; KV w:/pw:/m: values are read/report projections. This replaces an invalid dependence on KV propagation or atomic increments while preserving the existing key scheme.
- (decision) Request scope owns claims/cache identity and effective mode owns rendering: a domain downgrade stays domain-scoped, never pollutes global w:, and domain result access remains bounded by the Justin-owned Story 2.7 TTL decision.
- (decision) The six-call ledger is the hard cap; pair reservations and route-entry deadline include commit reserve. NeuronMeter remains best-effort/fail-open by existing convention and never creates additional capacity.
- (change) Qualification is manifest- and activation-hash-bound across structural, semantic, and full-pair evidence; evidence-v2 is additive and runtime mismatch disables the role.
- (version) Tech-currency review found current wrangler 4.123.0 versus project ^4.114.0 and invalid preview_urls placement under observability; both are deferred implementation/config maintenance, with offline dev and isolated approval-bound live spikes required in the meantime.
- (event) Tech-currency re-review PASS; synchronized the diagram, Structural Seed, and Capability Map with COORD authority and AD-8 atomic metrics.
- (change) Adversarial/rubric re-review closure corrected the port sequence so grounding is derived after generation, closed the tagged Evidence schemas, specified qualification hashing and production role transitions, and made the failure-precedence table authoritative.
- (decision) Mixed-version rollout is reader-first with rollback limited to readers that understand or reject the new envelope; no new KV namespace or owner-deferred TTL value was invented.
- (change) AD-8 now binds only aggregate operational/SM-1 measurement; SM-2 sampled integrity and SM-3 attributed conversations retain their separate human measurement authorities.
- (event) Final reviewer gate PASS: deterministic lint reported zero findings; rubric, tech-currency, and adversarial lenses reported no remaining build blocker.
- (event) Architecture course-correction reconciliation finalized for epic/story handoff.
- (change) Approved epic assumption audit clarified AD-8's denominator: authoritative `briefs_served`, house-only subset `house_briefs_served`, and accepted `invitation_acted`; 400/502 responses enter no served-outcome counter.
- (decision) Party-mode owner choice: SM-1 stays privacy-preserving and aggregate as `invitation_acted / briefs_served`, explicitly approximate and non-deduplicated; no per-render token or session-like mechanism is added.
- (decision) Party-mode release challenge: preserve story numbers but execute by dependency. Story 1.10 is reader-first then writer; Story 1.9 is offline implementation then post-1.10/post-1.13 full-pair evidence; Story 1.2 judge and Story 1.7 generation structural gates remain independent; activation is atomic and last.
- (correction) Epic reconciliation confirmed the preserved domain TTL story is 2.6, not 2.7; all architecture and handoff references were corrected without renumbering. Story 2.7 remains legacy-seam deletion.
- (decision) Story 2.2 design choice B: retain one model-assisted domain Evidence call (`E=1`) behind a separately qualified `EvidenceProvider`; its structural evidence cannot borrow generation or judge qualification, and domain activation also requires full-request evidence including Evidence.
2026-08-17: Justin selected option A for Story 2.6: domain results expire exactly one hour after authoritative commit. `/s/:id` always refuses domain artifacts; the one-hour boundary governs the coordinator receipt, `pw:` projection, and `/api/spark/:id` eligibility. The separate profile cache remains 24 hours.
2026-08-17: Justin selected option A for Story 3.5: public launch requires `house_briefs_served / briefs_served` strictly below 10% over at least 100 authoritative production serve events. Use integer comparison; do not relabel it as a unique-visitor or model-failure percentage.
2026-08-17: Justin selected option A for Story 3.6: local public `/s/:id` artifacts and their authoritative receipts expire exactly 30 days after commit. Domain-scoped artifacts remain fixed at one hour and never gain permalink eligibility.
2026-08-17: Justin selected option A for Story 3.7: accepted invitation actions redirect server-side to the Hearn Systems contact form with an opaque spark id. The hearn.systems form integration is a separate repository/authorization boundary; no Brief text, domain, Evidence, or visitor data enters the URL.
2026-08-17: Cascading-failure elicitation hardening approved. Domain receipt verification now uses exactly ten synchronized requests total to respect the visitor limit. Story 3.5 is a quiet-production gate for promotion, claim activation, and destructive seam retirement. Hearn handoff promises only an opaque lifetime-bounded reference and fails back to a plain contact link when its receiver activation record is unavailable.
2026-08-17: Assumption-audit hardening approved. Story 3.4 now requires all ten domain rate-limit slots unused before its exact ten-request burst. Hearn receiver activation uses a closed hash-bound manifest. Domain production reviews occur only inside ordinary one-hour eligibility and may remain incomplete. Slow launch-sample accumulation is schedule state, not permission to manufacture traffic. SM-1 reports disclose receiver activation coverage.
2026-08-17: Pre-mortem hardening approved. Story 3.6's reviewed toolchain/runtime baseline now precedes live qualification and later runtime changes invalidate evidence transitively. Production activation is one closed atomic manifest; receipt claims require an exact current claim-proof ref. Story 3.5 pairs COORD deltas with complete same-interval HTTP outcomes and blocks on unexplained 5xx or incomplete coverage. Mixed stories remain in progress until every required slice passes.
2026-08-17: Stakeholder-lens hardening approved. Invitation UI discloses lifetime-bounded reference availability. Invalid activation configuration degrades through the authoritative committed/approved-house path instead of crashing the shell. Launch HTTP evidence is aggregate-only and privacy-safe. Hearn proof becomes stale on either side's relevant contract/deployment behavior change. One deterministic release-decision report exposes every gate while preserving separate approval boundaries.
2026-08-17: Subtraction pass approved. Flattened production activation so shared qualification refs appear once, made the canonical manifest the sole deployed activation value with a derived hash, replaced countdown-like invitation wording with fixed expiry disclosure, made HTTP availability evidence interval-based, and prohibited new persistence for decision views or activation diagnostics.
2026-08-17: Party review closed the flattened-identity question without an owner decision. Story 2.4 already requires the exact Story 1.7 structural identity; acceptance criteria now mechanically compare shared hashes across modes and reject mode-specific generation, judge, semantic, prompt, adapter, or parameter overrides. Story 3.6 fixtures cover that rejection.
2026-08-17: Final-validation remediation authorized. The 28 approved capability stories were resliced into 42 single-agent, dependency-ordered stories with explicit FR traceability and a legacy-ID crosswalk. Product contracts and owner decisions remain unchanged; current IDs now align toolchain, structural, semantic, full-request, activation, production proof, quiet observation, claim, Hearn, and destructive-retirement sequencing.
