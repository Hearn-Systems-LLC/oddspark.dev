# Work Packet — Story 1.19 Local Full-Request Qualification harness (development)

## Outcome
Build the offline half of Story 1.19 per `_bmad-output/implementation-artifacts/spec-1-19-local-full-request-qualification.md` (read it first; it is the authority; the epic context `_bmad-output/implementation-artifacts/epic-1-context.md` and approved `_bmad-output/planning-artifacts/sprint-change-proposal-2026-08-24-4.md` bind too):

- Create `spikes/local-full-request-qualification/` — a governed harness in the shape of `spikes/generation-qualification/` (study its `contract.mjs`, `run.mjs`, `evidence-v2.mjs`, `verify-v2.mjs`, `recovery-finder.mjs`, `start-adapter.mjs`, `test.mjs`, `README.md` and mirror the governance): closed plan/approval schema, exclusive stale-safe cycle lock, fsynced call accounting, append-only attempt history, immutable evidence retention, and an independent arbitrary-byte verifier.
- The runner drives the **real assembled pipeline** — import the Story 1.23 assembly path (`src/pipeline/assembly.mjs` and the modules it assembles, including `strike.mjs`, `gate.mjs`, `judge.mjs`, `production-ports.mjs`) unchanged — through an adapter worker under isolated nonproduction config, so one local request exercises Evidence → generate → deterministic gate → ≤1 judge call/attempt → bounded attempts → house fallback → authoritative commit → render. The provider envelope (`production-ports.mjs`) is the wire shape under test.
- Frozen predicate set (all must be independently re-verifiable from retained bytes): model-call accounting within the six-call ledger; ≤3 local attempts; at most one candidate-bound judge call per attempt; deterministic rejection consumes zero judge calls; house Brief never judged; commit reserve and route-entry deadline observed; receipt identity and authoritative commit confirmed; per-stage latency/timeout, usage, cost, and hashes retained; no retry outside the orchestrator; plan/approval/assembly/ref identity binding (assembly identity `02fb91201bcdde62f9f1386b4011d0f0d4440ad3a559a4d6503f2207455e1ea2`, STRUCT-GENERATION `34731e26b1c1ef79acd444ba8e775143d9a616c3ab915f52481bd81475796bfc` / role `5cf5a547b29d31304af686c610da9c4c5959299faf12d434db28493de92404b1`, STRUCT-JUDGE `7dc1ec98a625a1dd16f1166067b496e4209a415e7f10854ff781f46d0d0062d0`).
- Add `package.json` scripts `spike:full-request:plan`, `spike:full-request:self-test`, `spike:full-request:verify` following existing spike naming. Do NOT wire any live entrypoint into `check`, `test`, or CI.
- Offline tests cover the spec's I/O matrix refusal/accounting/failure-preservation cases with mocks only: zero-call preflight refusal, plan tamper detection, over-cap accounting, unbound judge call, missing commit reserve, identity mismatch, house-never-judged, ambiguous-attempt terminalization, independent verification of retained synthetic evidence.
- Prepare the exact **unapproved** live plan bundle (run ID, route ceiling, call/cost caps with current model pricing, retained-field list, schedule, predicate list) written to an OS temp location or the spike's `plans/` with null approval/execution and `allowance_consumed: false`. NO provider call, adapter start against a live binding, deployment, or activation.

## Worktree and scope
- Work only in this worktree: `.bmad-governor/worktrees/1-19-local-full-request-qualification` (branch `governor/1-19-local-full-request-qualification`).
- Allowed writes: `spikes/local-full-request-qualification/**`, `package.json` (scripts only), `_bmad-output/implementation-artifacts/spec-1-19-local-full-request-qualification.md` (status/change-log/tasks only), and this packet's handoff file. One specifically authorized `src/` exception: wiring the qualified judge descriptor in `src/pipeline/production-ports.mjs` — constructing the closed `PIPELINE_JUDGE` descriptor `{role:"STRUCT-JUDGE", provider, resolved_model, qualification_ref:"7dc1ec98a625a1dd16f1166067b496e4209a415e7f10854ff781f46d0d0062d0", status:"active", outcome:"GO"}` that `gate.mjs` requires (prior planning finding: this wiring is Story 1.19 scope), plus its tests in `test.mjs` and the assembly re-freeze (`npm run assembly:freeze`) if module bytes change. Second authorization (governor, 2026-08-24, after Justin's priors approval): update the test fixtures made stale by the approved priors — `test.mjs` Story 1.23/1.25 fixtures, `scripts/writer-preflight.test.mjs`, and the readiness wording in `scripts/writer-preflight.mjs` only if its own test requires it; assertions must be strengthened to the approved state (bundled env constructs incl. PIPELINE_JUDGE; writer still null without ACTIVATION_MANIFEST; parity and drift fail-closed assertions intact), never weakened or deleted. No other `src/` change: if any other src edit appears required, stop and report `blocked` with evidence instead.
- Forbidden: `src/**` except the single authorized `production-ports.mjs` PIPELINE_JUDGE wiring above, `sprint-status.yaml`, `wrangler*.toml`, `.github/`, `.env*`, `content/**`, `semantic/**`, other spikes' directories and their pinned evidence/results, `deferred-work.md`, any planning artifact (the uncommitted `epic-1-context.md` edit is intentional — leave it as-is).
- Do NOT commit, push, deploy, call any provider, or mutate remote state. Leave changes uncommitted.

## Validation gates (all must pass)
`npm test`, `npm run spike:full-request:self-test`, `npm run check` (the DW-6 evidence-pinned judge self-test residual at `spikes/judge-fidelity/test.mjs:1105` is pre-existing and excepted — confirm it is the only `check` failure and that it reproduces without your changes), `CI=1 node .github/check-ci.mjs`, `npm run assembly:verify`, `git diff --check`.

## Terminal handoff
Write `_bmad-output/implementation-artifacts/handoff-1-19-full-request-dev.md` with: status (`done`|`blocked`), files changed, exact commands run with pass/fail counts, the unapproved plan bundle location and its exact byte hash, and any deviation from this packet. Stop when it is written. Stop and report `blocked` instead of widening scope if the gates cannot pass within the allowed writes.

## Repair addendum (governor, 2026-08-24, post-review)
Independent review verdict `approve` with one low patch finding (governor-verified): `scripts/writer-preflight.mjs` asserts PIPELINE_JUDGE absence, stale after the authorized wiring. Additional allowed writes: `scripts/writer-preflight.mjs` (assert the exact closed descriptor instead of absence; fix stale comment) and `scripts/writer-preflight.test.mjs` (matching strengthened fixtures; tampered descriptor must still fail the gate). Re-run writer:preflight, its test, npm test, spike self-test, assembly:verify, git diff --check; append repair note to the dev handoff.
