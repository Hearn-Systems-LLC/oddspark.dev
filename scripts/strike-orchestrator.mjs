// Shim: the canonical runtime-neutral implementation moved to
// src/pipeline/strike.mjs (Story 1.23). This file only re-exports it for
// Node CLI/test consumers; it implements nothing itself.
export * from "../src/pipeline/strike.mjs";
